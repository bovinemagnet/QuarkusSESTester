# QuarkusSESTester
A Quarkus CLI utility that builds a raw MIME email with Simple Java Mail and submits it to Amazon SES over HTTPS, supporting dry-run mode, .env configuration, and realistic sender-domain testing under SES/SPF/DMARC constraints.
